function soma(x,y) {
    return x = y;
}

const multiplicação = function(x,y){
    return x * y;
};

// arrow function

const dividir = (x,y) => { return x/y;
};

//Objetos

const objeto = () => ({ nome: 'Camila', sobrenome: 'Barros'});